<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@+id/sync_li">

    <fragment
        android:id="@+id/sync_li"
        android:name="com.github.jirobird.syncli.screens.syncli.SyncLiFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_syncli">
    </fragment>
    <fragment
        android:id="@+id/shared_li"
        android:name="com.github.jirobird.syncli.screens.sharedli.SharedLiFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_sharedli">
    </fragment>
    <fragment
        android:id="@+id/never_buy_this_shit_again"
        android:name="com.github.jirobird.syncli.screens.unwanted_purchases.UnwantedPurchaseFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_unwanted_purchase">
    </fragment>
</navigation>